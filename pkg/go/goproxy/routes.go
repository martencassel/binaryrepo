package goproxy

/*import (
	"net/http"

	"github.com/gorilla/mux"
)

func registerRoutes(r *mux.Router) {
	r.HandleFunc("{module:.+}/@v/list", GoModuleList).Methods(http.MethodGet)
	r.HandleFunc("{module:.+}/@v/latest", GoModuleLatest).Methods(http.MethodGet)
	r.HandleFunc("{module:.+}/@v/{version}.info", GoModuleVersion).Methods(http.MethodGet)
	r.HandleFunc("{module:.+}/@v/{version}.mod", GoModuleMod).Methods(http.MethodGet)
	r.HandleFunc("{module:.+}/@v/{version}.zip", GoModuleZip).Methods(http.MethodGet)
}
*/
